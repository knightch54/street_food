<%= form_with(model: food_ingredient) do |form| %>
  <% if food_ingredient.errors.any? %>
    <div class="form-group">
      <p><%= pluralize(food_ingredient.errors.count, "error") %> prohibited this food ingredient from being saved:</p>
      <ul class="list-group mt-5 mb-5">
        <% food_ingredient.errors.each do |error| %>
          <li class="list-group-item"><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= form.collection_select :ingredient_id, Ingredient.all, :id, :name, {}, {multiple: false, class: "form-select form-control"} %>
  </div>
  <div class="form-group">
     <%= form.collection_select :food_id, Food.all, :id, :name, {}, {multiple: false, class: "form-select form-control"} %>
  </div>
  <div class="form-group">
    <%= form.submit class: "btn form-control btn-primary submit px-3 mt-5" %>
  </div>
<% end %>

